#include <stdio.h>

int main() {
    int a;
    int sum = a + 5; // Addition operator
    int diff = a - sum; // Subtraction operator
    int prod = a * diff; // Multiplication operator
    float div = (float)a / prod; // Division operator

    scanf("%d", &a); // Input operator
    printf("Sum: %d\n", sum); // Output operator

    return 0;
}